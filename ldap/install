1. 主要安装一下套件
apt-get install slapd ldap-utils migrationtools

2. 启动服务：
# /etc/init.d/slapd start

3. 编译安装ldap
找到php安装源码，在*/php/ext/ldap
/phpize
./configure --with-php-config=/usr/local/php/bin/php-config
make 
make install
##############
在php/lib/php.ini
extension=ldap.so
